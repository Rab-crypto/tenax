name: Test Install Scripts

on:
  push:
    paths:
      - 'install.sh'
      - 'install.ps1'
      - '.github/workflows/test-install.yml'
  pull_request:
    paths:
      - 'install.sh'
      - 'install.ps1'
  workflow_dispatch:

jobs:
  test-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate bash syntax
        run: bash -n install.sh

      - name: Run shellcheck
        run: |
          sudo apt-get update && sudo apt-get install -y shellcheck
          shellcheck install.sh || true

      - name: Test install script
        run: |
          # Create mock claude directory structure
          mkdir -p ~/.claude

          # Create mock settings.json
          echo '{}' > ~/.claude/settings.json

          # Install jq (needed for permissions)
          sudo apt-get install -y jq

          # Run installer (will clone repo, npm install, etc.)
          bash install.sh

          # Verify installation
          echo "=== Checking installation ==="
          [ -d ~/.claude/plugins/tenax ] && echo "✓ Tenax directory exists" || echo "✗ Tenax directory missing"
          [ -f ~/.claude/plugins/tenax/package.json ] && echo "✓ package.json exists" || echo "✗ package.json missing"
          [ -d ~/.claude/plugins/tenax/node_modules ] && echo "✓ node_modules exists" || echo "✗ node_modules missing"

          # Check permissions were added
          echo "=== Checking permissions ==="
          cat ~/.claude/settings.json | jq '.permissions.allow'

  test-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate bash syntax
        run: bash -n install.sh

      - name: Test install script
        run: |
          mkdir -p ~/.claude
          echo '{}' > ~/.claude/settings.json
          bash install.sh

          echo "=== Checking installation ==="
          [ -d ~/.claude/plugins/tenax ] && echo "✓ Tenax directory exists" || echo "✗ Tenax directory missing"
          [ -d ~/.claude/plugins/tenax/node_modules ] && echo "✓ node_modules exists" || echo "✗ node_modules missing"

  test-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate PowerShell syntax
        shell: pwsh
        run: |
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
            "$PWD\install.ps1",
            [ref]$null,
            [ref]$errors
          ) | Out-Null
          if ($errors) {
            $errors | ForEach-Object { Write-Error $_ }
            exit 1
          }
          Write-Host "✓ PowerShell syntax valid"

      - name: Test install script
        shell: pwsh
        run: |
          # Create mock claude directory
          New-Item -ItemType Directory -Path "$env:USERPROFILE\.claude" -Force | Out-Null

          # Create mock settings.json
          '{}' | Set-Content "$env:USERPROFILE\.claude\settings.json"

          # Run installer with verbose output
          try {
            & .\install.ps1
          } catch {
            Write-Host "Install script error: $_"
            Write-Host $_.ScriptStackTrace
            exit 1
          }

          # Verify installation
          Write-Host "=== Checking installation ==="
          if (Test-Path "$env:USERPROFILE\.claude\plugins\tenax") {
            Write-Host "✓ Tenax directory exists"
            Get-ChildItem "$env:USERPROFILE\.claude\plugins\tenax" | Select-Object Name
          } else {
            Write-Host "✗ Tenax directory missing"
            exit 1
          }

          if (Test-Path "$env:USERPROFILE\.claude\plugins\tenax\node_modules") {
            Write-Host "✓ node_modules exists"
          } else {
            Write-Host "✗ node_modules missing"
            exit 1
          }

          # Check settings
          Write-Host "=== Checking settings.json ==="
          Get-Content "$env:USERPROFILE\.claude\settings.json" | ConvertFrom-Json | ConvertTo-Json -Depth 10
